{
  "_comment": "Server connection settings live in %LOCALAPPDATA%\\NTNP\\Pricing\\client-settings.json (see ServerConnectionSettingsService) so the same installed client can be repointed between environments without a reinstall. This file is reserved for future non-user-editable desktop defaults."
}
