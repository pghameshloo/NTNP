<UserControl x:Class="NTNP.Pricing.Desktop.Views.ServerConnectionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ServerConnectionSettingsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <StackPanel MaxWidth="560" HorizontalAlignment="Left">
        <TextBlock Text="تنظیمات اتصال به سرور" Style="{StaticResource DisplayTextStyle}" Margin="0,0,0,20" />

        <Border Style="{StaticResource CardBorderStyle}">
            <StackPanel>
                <TextBlock Text="آدرس سرور (Base URL)" Style="{StaticResource CaptionTextStyle}" />
                <TextBox Text="{Binding ApiBaseUrl}" FlowDirection="LeftToRight" TextAlignment="Left" Margin="0,4,0,16" />

                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Button Content="آزمایش اتصال" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding TestConnectionCommand}" Margin="0,0,10,0" />
                    <Button Content="ذخیره" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Ellipse Width="9" Height="9" Margin="0,0,7,0" VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="{StaticResource ErrorBrush}" />
                                <Style.Triggers><DataTrigger Binding="{Binding ConnectionOk}" Value="True"><Setter Property="Fill" Value="{StaticResource SuccessBrush}" /></DataTrigger></Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding ConnectionStatus}" Style="{StaticResource CaptionTextStyle}" />
                </StackPanel>

                <TextBlock Text="{Binding ApiVersion, StringFormat='نسخه API: {0}'}" Style="{StaticResource CaptionTextStyle}" Visibility="{Binding ApiVersion, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,4" />
                <TextBlock Text="{Binding DatabaseSchemaVersion, StringFormat='نسخه پایگاه‌داده: {0}'}" Style="{StaticResource CaptionTextStyle}" Visibility="{Binding DatabaseSchemaVersion, Converter={StaticResource TruthyToVisibility}}" />
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>
