<UserControl x:Class="NTNP.Pricing.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="KpiCardStyle" TargetType="Border" BasedOn="{StaticResource CardBorderStyle}">
            <Setter Property="Padding" Value="20,16" />
            <Setter Property="Margin" Value="0,0,14,14" />
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <ProgressBar IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />

            <TextBlock Text="نمای کلی سامانه" Style="{StaticResource DisplayTextStyle}" Margin="0,0,0,20" />

            <!-- KPI cards -->
            <UniformGrid Columns="4" Rows="2">
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="پروژه‌های فعال" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.ActiveProjectsCount}" Style="{StaticResource DisplayTextStyle}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="پیش‌نویس‌های قیمت" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.DraftQuotationsCount}" Style="{StaticResource DisplayTextStyle}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="در انتظار تأیید" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.PendingApprovalsCount}" Style="{StaticResource DisplayTextStyle}" Foreground="{StaticResource WarningBrush}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="تأیید شده" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.ApprovedQuotationsCount}" Style="{StaticResource DisplayTextStyle}" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="تجهیزات بدون قیمت" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.EquipmentMissingPriceCount}" Style="{StaticResource DisplayTextStyle}" Foreground="{StaticResource ErrorBrush}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="نرخ ارز منقضی" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.ExpiredExchangeRatesCount}" Style="{StaticResource DisplayTextStyle}" Foreground="{StaticResource WarningBrush}" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="ارزش کل پیشنهادها (ریال)" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.TotalQuotationValueIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="22" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}">
                    <StackPanel>
                        <TextBlock Text="میانگین حاشیه سود" Style="{StaticResource CaptionTextStyle}" />
                        <TextBlock Text="{Binding Summary.AverageGrossMargin, Converter={StaticResource Percent}}" Style="{StaticResource NumericTextStyle}" FontSize="22" Margin="0,8,0,0" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Projects by status -->
                <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="پروژه‌ها بر اساس وضعیت" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />
                        <ItemsControl ItemsSource="{Binding ProjectsByStatusBars}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="40" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Label, Converter={StaticResource StatusToPersianLabel}}" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                                        <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}" CornerRadius="4" Height="16" HorizontalAlignment="Stretch">
                                            <Border Background="{StaticResource ElectricBlueBrush}" CornerRadius="4" Height="16" HorizontalAlignment="Left"
                                                    Width="{Binding FractionOfMax, Converter={StaticResource FractionToWidth}, ConverterParameter=300}" />
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding ValueText}" Style="{StaticResource MonoLabelTextStyle}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Cost composition -->
                <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="ترکیب هزینه (ریال)" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />
                        <ItemsControl ItemsSource="{Binding CostCompositionBars}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="120" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="90" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Label}" Style="{StaticResource BodyTextStyle}" VerticalAlignment="Center" />
                                        <Border Grid.Column="1" Background="{StaticResource BackgroundBrush}" CornerRadius="4" Height="16" HorizontalAlignment="Stretch">
                                            <Border Background="{StaticResource IndustrialCyanBrush}" CornerRadius="4" Height="16" HorizontalAlignment="Left"
                                                    Width="{Binding FractionOfMax, Converter={StaticResource FractionToWidth}, ConverterParameter=300}" />
                                        </Border>
                                        <TextBlock Grid.Column="2" Text="{Binding ValueText}" Style="{StaticResource MonoLabelTextStyle}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Recent projects -->
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="پروژه‌های اخیر" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,12" />
                    <DataGrid ItemsSource="{Binding Summary.RecentProjects}" MaxHeight="260">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="کد پروژه" Binding="{Binding ProjectCode}" Width="120" />
                            <DataGridTextColumn Header="نام پروژه" Binding="{Binding ProjectName}" Width="*" />
                            <DataGridTextColumn Header="مشتری" Binding="{Binding CustomerName}" Width="180" />
                            <DataGridTemplateColumn Header="وضعیت" Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource BadgeInfoStyle}">
                                            <TextBlock Text="{Binding Status, Converter={StaticResource StatusToPersianLabel}}" Style="{StaticResource BadgeTextStyle}" />
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="آخرین بروزرسانی" Binding="{Binding UpdatedAtUtc, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}" Width="160" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
