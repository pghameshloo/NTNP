<UserControl x:Class="NTNP.Pricing.Desktop.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ProjectListViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="پروژه‌های قیمت‌گذاری" Style="{StaticResource DisplayTextStyle}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="220" VerticalAlignment="Center" KeyDown="SearchBox_KeyDown" />
            <ComboBox ItemsSource="{Binding StatusOptions}" SelectedValue="{Binding StatusFilter}" SelectedValuePath="Value" DisplayMemberPath="Label" Width="160" Margin="10,0,0,0" VerticalAlignment="Center" />
            <Button Content="جستجو" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding SearchCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Projects}" MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="کد پروژه" Binding="{Binding ProjectCode}" Width="110" />
                    <DataGridTextColumn Header="نام پروژه" Binding="{Binding ProjectName}" Width="*" />
                    <DataGridTextColumn Header="مشتری" Binding="{Binding CustomerName}" Width="180" />
                    <DataGridTemplateColumn Header="وضعیت" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource BadgeInfoStyle}">
                                    <TextBlock Text="{Binding Status, Converter={StaticResource StatusToPersianLabel}}" Style="{StaticResource BadgeTextStyle}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="نسخه فعلی" Binding="{Binding CurrentRevisionNumber}" Width="90" />
                    <DataGridTemplateColumn Header="مبلغ کل (ریال)" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate><TextBlock Text="{Binding TotalProjectSellingPriceIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="آخرین بروزرسانی" Binding="{Binding UpdatedAtUtc, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}" Width="150" />
                    <DataGridTemplateColumn Header="" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="باز کردن" Style="{StaticResource SecondaryButtonStyle}" Padding="10,4"
                                        Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
