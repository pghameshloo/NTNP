<UserControl x:Class="NTNP.Pricing.Desktop.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:UsersViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="کاربران و سطوح دسترسی" Style="{StaticResource DisplayTextStyle}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <Button Content="بازخوانی" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding LoadCommand}" Margin="0,0,10,0" />
            <Button Content="+ کاربر جدید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NewCommand}" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام کاربری" Binding="{Binding UserName}" Width="140" />
                        <DataGridTextColumn Header="نام نمایشی" Binding="{Binding DisplayName}" Width="*" />
                        <DataGridTextColumn Header="ایمیل" Binding="{Binding Email}" Width="180" />
                        <DataGridTemplateColumn Header="وضعیت" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Border Style="{StaticResource BadgeSuccessStyle}" Visibility="{Binding IsActive, Converter={StaticResource TruthyToVisibility}}"><TextBlock Text="فعال" Style="{StaticResource BadgeTextStyle}" /></Border>
                                        <Border Style="{StaticResource BadgeErrorStyle}" Visibility="{Binding IsActive, Converter={StaticResource FalsyToVisibility}}"><TextBlock Text="غیرفعال" Style="{StaticResource BadgeTextStyle}" /></Border>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource TruthyToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="اطلاعات کاربر" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />

                        <TextBlock Text="نام کاربری/ایمیل *" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormUserName}" IsEnabled="{Binding IsNew}" FlowDirection="LeftToRight" TextAlignment="Right" Margin="0,4,0,12" />
                        <TextBlock Text="ایمیل *" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormEmail}" IsEnabled="{Binding IsNew}" FlowDirection="LeftToRight" TextAlignment="Right" Margin="0,4,0,12" />
                        <TextBlock Text="نام نمایشی *" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormDisplayName}" Margin="0,4,0,12" />

                        <TextBlock Text="نقش‌ها *" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6" />
                        <CheckBox Content="مدیر سیستم (Admin)" IsChecked="{Binding FormIsAdmin}" Margin="0,0,0,6" />
                        <CheckBox Content="مهندسی (Engineering)" IsChecked="{Binding FormIsEngineering}" Margin="0,0,0,6" />
                        <CheckBox Content="بازرگانی (Commercial)" IsChecked="{Binding FormIsCommercial}" Margin="0,0,0,6" />
                        <CheckBox Content="تأییدکننده (Approver)" IsChecked="{Binding FormIsApprover}" Margin="0,0,0,6" />
                        <CheckBox Content="بیننده (Viewer)" IsChecked="{Binding FormIsViewer}" Margin="0,0,0,12" />

                        <CheckBox Content="فعال" IsChecked="{Binding FormIsActive}" Visibility="{Binding IsNew, Converter={StaticResource FalsyToVisibility}}" Margin="0,0,0,16" />

                        <TextBlock Style="{StaticResource CaptionTextStyle}">
                            <Run Text="رمز عبور" />
                            <Run Text="(برای بازنشانی وارد کنید)" FontSize="11" />
                        </TextBlock>
                        <TextBox Text="{Binding FormPassword}" FlowDirection="LeftToRight" Margin="0,4,0,12" />
                        <Button Content="بازنشانی رمز عبور" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ResetPasswordCommand}"
                                HorizontalAlignment="Left" Visibility="{Binding IsNew, Converter={StaticResource FalsyToVisibility}}" Margin="0,0,0,16" />

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />
                        <StackPanel Orientation="Horizontal">
                            <Button Content="ذخیره" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" Margin="0,0,10,0" />
                            <Button Content="انصراف" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelEditCommand}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource FalsyToVisibility}}">
                <TextBlock Text="برای مشاهده یا ویرایش، یک کاربر را از فهرست انتخاب کنید." Style="{StaticResource SubheadingTextStyle}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
