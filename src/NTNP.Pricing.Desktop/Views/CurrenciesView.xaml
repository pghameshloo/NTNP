<UserControl x:Class="NTNP.Pricing.Desktop.Views.CurrenciesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:CurrenciesViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="نرخ ارز" Style="{StaticResource DisplayTextStyle}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <CheckBox Content="نمایش غیرفعال‌ها" IsChecked="{Binding IncludeInactive}" VerticalAlignment="Center" />
            <Button Content="بازخوانی" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding LoadCommand}" Margin="10,0,0,0" />
            <Button Content="+ ارز جدید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ShowAddCurrencyFormCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="1.2*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="کد" Binding="{Binding Code}" Width="70" />
                        <DataGridTextColumn Header="نام" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="نماد" Binding="{Binding Symbol}" Width="60" />
                        <DataGridTemplateColumn Header="نرخ خرید" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding LatestRate.PurchaseRateToIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="نرخ فروش" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding LatestRate.SellingRateToIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="وضعیت نرخ" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource BadgeWarningStyle}" Visibility="{Binding LatestRate.IsStale, Converter={StaticResource TruthyToVisibility}}">
                                        <TextBlock Text="منقضی" Style="{StaticResource BadgeTextStyle}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding ShowAddCurrency, Converter={StaticResource TruthyToVisibility}}">
                <StackPanel>
                    <TextBlock Text="افزودن ارز جدید" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />
                    <TextBlock Text="کد ارز (ISO 4217)" Style="{StaticResource CaptionTextStyle}" />
                    <TextBox Text="{Binding NewCurrencyCode}" FlowDirection="LeftToRight" Margin="0,4,0,12" MaxLength="3" />
                    <TextBlock Text="نام ارز" Style="{StaticResource CaptionTextStyle}" />
                    <TextBox Text="{Binding NewCurrencyName}" Margin="0,4,0,12" />
                    <TextBlock Text="نماد" Style="{StaticResource CaptionTextStyle}" />
                    <TextBox Text="{Binding NewCurrencySymbol}" Margin="0,4,0,16" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />
                    <StackPanel Orientation="Horizontal">
                        <Button Content="ذخیره" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddCurrencyCommand}" Margin="0,0,10,0" />
                        <Button Content="انصراف" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelAddCurrencyCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding SelectedCurrency, Converter={StaticResource TruthyToVisibility}}">
                <StackPanel Visibility="{Binding ShowAddCurrency, Converter={StaticResource FalsyToVisibility}}">
                    <TextBlock Text="{Binding SelectedCurrency.Name}" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,4" />
                    <TextBlock Text="ثبت نرخ ارز جدید" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,16" />

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0"><TextBlock Text="نرخ خرید (ریال)" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding NewRatePurchase}" FlowDirection="LeftToRight" Margin="0,4,0,0" /></StackPanel>
                        <StackPanel Grid.Column="2"><TextBlock Text="نرخ فروش (ریال)" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding NewRateSelling}" FlowDirection="LeftToRight" Margin="0,4,0,0" /></StackPanel>
                    </Grid>
                    <TextBlock Text="تاریخ اعتبار" Style="{StaticResource CaptionTextStyle}" />
                    <DatePicker SelectedDate="{Binding NewRateEffectiveAt}" Margin="0,4,0,12" />
                    <TextBlock Text="منبع نرخ" Style="{StaticResource CaptionTextStyle}" />
                    <TextBox Text="{Binding NewRateSource}" Margin="0,4,0,12" />
                    <TextBlock Text="یادداشت" Style="{StaticResource CaptionTextStyle}" />
                    <TextBox Text="{Binding NewRateNotes}" Margin="0,4,0,12" />
                    <Button Content="ثبت نرخ" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddRateCommand}" HorizontalAlignment="Left" Margin="0,0,0,16" />

                    <Separator Margin="0,0,0,16" />
                    <TextBlock Text="تاریخچه نرخ" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,10" />
                    <DataGrid ItemsSource="{Binding RateHistory}" MaxHeight="260">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="خرید" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate><TextBlock Text="{Binding PurchaseRateToIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="فروش" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate><TextBlock Text="{Binding SellingRateToIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="تاریخ" Binding="{Binding EffectiveAtUtc, StringFormat='{}{0:yyyy/MM/dd}'}" Width="110" />
                            <DataGridTextColumn Header="ثبت‌کننده" Binding="{Binding CreatedByUserName}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
