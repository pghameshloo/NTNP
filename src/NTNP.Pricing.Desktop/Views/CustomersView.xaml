<UserControl x:Class="NTNP.Pricing.Desktop.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:CustomersViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="مشتریان" Style="{StaticResource DisplayTextStyle}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="260" VerticalAlignment="Center" KeyDown="SearchBox_KeyDown" />
            <CheckBox Content="نمایش غیرفعال‌ها" IsChecked="{Binding IncludeInactive}" Margin="14,0,0,0" VerticalAlignment="Center" />
            <Button Content="جستجو" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding SearchCommand}" Margin="10,0,0,0" />
            <Button Content="+ مشتری جدید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NewCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.4*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="کد" Binding="{Binding CustomerCode}" Width="90" />
                        <DataGridTextColumn Header="نام شرکت" Binding="{Binding CompanyName}" Width="*" />
                        <DataGridTextColumn Header="رابط" Binding="{Binding ContactPerson}" Width="140" />
                        <DataGridTextColumn Header="تلفن" Binding="{Binding Phone}" Width="120" />
                        <DataGridTemplateColumn Header="وضعیت" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource BadgeSuccessStyle}" Visibility="{Binding IsActive, Converter={StaticResource TruthyToVisibility}}">
                                        <TextBlock Text="فعال" Style="{StaticResource BadgeTextStyle}" />
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource TruthyToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="اطلاعات مشتری" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />

                        <TextBlock Text="کد مشتری" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormCustomerCode}" IsEnabled="{Binding IsNew}" Margin="0,4,0,12" />

                        <TextBlock Text="نام شرکت *" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormCompanyName}" Margin="0,4,0,12" />

                        <TextBlock Text="حوزه فعالیت" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormIndustry}" Margin="0,4,0,12" />

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="شماره ثبت" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormRegistrationNumber}" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="شناسه مالیاتی" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormTaxId}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="نام رابط" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormContactPerson}" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="سمت رابط" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormContactPosition}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="12" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="تلفن" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormPhone}" FlowDirection="LeftToRight" TextAlignment="Right" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="ایمیل" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormEmail}" FlowDirection="LeftToRight" TextAlignment="Right" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="آدرس" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormAddress}" AcceptsReturn="True" TextWrapping="Wrap" Height="56" Margin="0,4,0,12" />

                        <TextBlock Text="یادداشت" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormNotes}" AcceptsReturn="True" TextWrapping="Wrap" Height="56" Margin="0,4,0,12" />

                        <CheckBox Content="فعال" IsChecked="{Binding FormIsActive}" Visibility="{Binding IsNew, Converter={StaticResource FalsyToVisibility}}" Margin="0,0,0,16" />

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap"
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />

                        <StackPanel Orientation="Horizontal">
                            <Button Content="ذخیره" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" Margin="0,0,10,0" />
                            <Button Content="انصراف" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelEditCommand}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource FalsyToVisibility}}">
                <TextBlock Text="برای مشاهده یا ویرایش، یک مشتری را از فهرست انتخاب کنید." Style="{StaticResource SubheadingTextStyle}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
