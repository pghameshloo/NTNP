<UserControl x:Class="NTNP.Pricing.Desktop.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:AuditLogViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="گزارش فعالیت‌ها" Style="{StaticResource DisplayTextStyle}" Margin="0,0,0,14" />
            <WrapPanel>
                <StackPanel Margin="0,0,14,0"><TextBlock Text="نوع موجودیت" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding EntityType}" Width="160" Margin="0,4,0,0" /></StackPanel>
                <StackPanel Margin="0,0,14,0"><TextBlock Text="شناسه موجودیت" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding EntityId}" Width="200" Margin="0,4,0,0" /></StackPanel>
                <StackPanel Margin="0,0,14,0"><TextBlock Text="از تاریخ" Style="{StaticResource CaptionTextStyle}" /><DatePicker SelectedDate="{Binding FromUtc}" Width="140" Margin="0,4,0,0" /></StackPanel>
                <StackPanel Margin="0,0,14,0"><TextBlock Text="تا تاریخ" Style="{StaticResource CaptionTextStyle}" /><DatePicker SelectedDate="{Binding ToUtc}" Width="140" Margin="0,4,0,0" /></StackPanel>
                <Button Content="جستجو" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SearchCommand}" VerticalAlignment="Bottom" />
            </WrapPanel>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Entries}" SelectedItem="{Binding SelectedEntry}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="زمان" Binding="{Binding AtUtc, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}" Width="140" />
                    <DataGridTextColumn Header="کاربر" Binding="{Binding UserName}" Width="110" />
                    <DataGridTextColumn Header="عملیات" Binding="{Binding Action, Converter={StaticResource StatusToPersianLabel}}" Width="120" />
                    <DataGridTextColumn Header="نوع موجودیت" Binding="{Binding EntityType}" Width="130" />
                    <DataGridTextColumn Header="شناسه" Binding="{Binding EntityId}" Width="200" />
                    <DataGridTextColumn Header="دلیل" Binding="{Binding Reason}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
