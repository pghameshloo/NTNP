<UserControl x:Class="NTNP.Pricing.Desktop.Views.PricingProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:PricingProfilesViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="پروفایل‌های قیمت‌گذاری" Style="{StaticResource DisplayTextStyle}" VerticalAlignment="Center" Margin="0,0,24,0" />
            <CheckBox Content="نمایش غیرفعال‌ها" IsChecked="{Binding IncludeInactive}" VerticalAlignment="Center" />
            <Button Content="بازخوانی" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding LoadCommand}" Margin="10,0,0,0" />
            <Button Content="+ پروفایل جدید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding NewCommand}" Margin="10,0,0,0" />
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorderStyle}" Padding="0">
                <DataGrid ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="نام پروفایل" Binding="{Binding Name}" Width="*" />
                        <DataGridTextColumn Header="روش قیمت‌گذاری" Binding="{Binding PricingMethod, Converter={StaticResource StatusToPersianLabel}}" Width="130" />
                        <DataGridTemplateColumn Header="نرخ پیش‌فرض" Width="110">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate><TextBlock Text="{Binding DefaultRate, Converter={StaticResource Percent}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="ارز پیش‌فرض" Binding="{Binding DefaultQuotationCurrencyCode}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource TruthyToVisibility}}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="مشخصات پروفایل" Style="{StaticResource HeadingTextStyle}" Margin="0,0,0,16" />

                        <TextBlock Text="نام پروفایل *" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormName}" Margin="0,4,0,12" />

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="روش قیمت‌گذاری" Style="{StaticResource CaptionTextStyle}" />
                                <ComboBox ItemsSource="{Binding PricingMethods}" SelectedItem="{Binding FormPricingMethod}" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="نرخ پیش‌فرض (کسری مانند 0.30)" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormDefaultRate}" FlowDirection="LeftToRight" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="سهم ریالی پیش‌فرض" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormDefaultRialShare}" FlowDirection="LeftToRight" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="سهم ارزی پیش‌فرض" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormDefaultForeignShare}" FlowDirection="LeftToRight" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="ارز پیش‌فرض پیشنهاد قیمت" Style="{StaticResource CaptionTextStyle}" />
                        <TextBox Text="{Binding FormDefaultQuotationCurrencyCode}" FlowDirection="LeftToRight" Margin="0,4,0,12" />

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="سیاست رند کردن ریالی" Style="{StaticResource CaptionTextStyle}" />
                                <ComboBox ItemsSource="{Binding RoundingModes}" SelectedItem="{Binding FormIrrRoundingPolicy}" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="سیاست رند کردن ارزی" Style="{StaticResource CaptionTextStyle}" />
                                <ComboBox ItemsSource="{Binding RoundingModes}" SelectedItem="{Binding FormForeignRoundingPolicy}" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="اعشار ارزی" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormForeignDecimalPlaces}" FlowDirection="LeftToRight" Margin="0,4,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="تلورانس تطبیق (ریال)" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding FormReconciliationToleranceIrr}" FlowDirection="LeftToRight" Margin="0,4,0,0" />
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,12" />
                        <StackPanel Orientation="Horizontal">
                            <Button Content="ذخیره" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCommand}" Margin="0,0,10,0" />
                            <Button Content="انصراف" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CancelEditCommand}" />
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}" Visibility="{Binding IsEditing, Converter={StaticResource FalsyToVisibility}}">
                <TextBlock Text="برای مشاهده یا ویرایش، یک پروفایل را از فهرست انتخاب کنید." Style="{StaticResource SubheadingTextStyle}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
