<UserControl x:Class="NTNP.Pricing.Desktop.Views.ProjectWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTNP.Pricing.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:ProjectWorkspaceViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" x:Name="Root">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Padding="18" Margin="0,0,0,16">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Project.ProjectName}" Style="{StaticResource HeadingTextStyle}" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding Project.ProjectCode, StringFormat=' ({0})'}" Style="{StaticResource SubheadingTextStyle}" VerticalAlignment="Center" Margin="6,0,0,0" />
                    <Border Style="{StaticResource BadgeInfoStyle}" Margin="16,0,0,0">
                        <TextBlock Text="{Binding Revision.Status, Converter={StaticResource StatusToPersianLabel}}" Style="{StaticResource BadgeTextStyle}" />
                    </Border>
                    <TextBlock Text="{Binding Revision.RevisionNumber, StringFormat='نسخه {0}'}" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Center" Margin="14,0,0,0" />
                </StackPanel>
                <ProgressBar IsIndeterminate="True" Width="140" HorizontalAlignment="Left" Visibility="{Binding IsBusy, Converter={StaticResource TruthyToVisibility}}" />
            </Grid>
        </Border>

        <TabControl Grid.Row="1">

            <!-- ===================== Tab: Project Lineup ===================== -->
            <TabItem Header="چیدمان پروژه">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16" Visibility="{Binding IsMutable, Converter={StaticResource TruthyToVisibility}}">
                            <StackPanel>
                                <TextBlock Text="افزودن ردیف تابلو (تولید خودکار BOM)" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="10" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" Text="{Binding TemplateSearchText, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="2" Content="جستجو" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding SearchTemplatesCommand}" />
                                </Grid>
                                <ComboBox ItemsSource="{Binding TemplateResults}" SelectedItem="{Binding SelectedTemplate}" DisplayMemberPath="TemplateName" Margin="0,0,0,10" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"><TextBlock Text="کد سلول *" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding CellCode}" Margin="0,4,0,0" /></StackPanel>
                                    <StackPanel Grid.Column="2"><TextBlock Text="تعداد" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding QuantityOfPanels}" FlowDirection="LeftToRight" Margin="0,4,0,0" /></StackPanel>
                                    <StackPanel Grid.Column="4"><TextBlock Text="سایر هزینه‌ها" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding OtherDirectCostPerPanel}" FlowDirection="LeftToRight" Margin="0,4,0,0" /></StackPanel>
                                </Grid>
                                <Button Content="+ افزودن ردیف" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddLineCommand}" HorizontalAlignment="Left" />
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource TruthyToVisibility}}" Margin="0,0,0,16" />

                        <DataGrid ItemsSource="{Binding Revision.Lines}" SelectedItem="{Binding SelectedLine}" MaxHeight="360">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ردیف" Binding="{Binding LineNumber}" Width="55" />
                                <DataGridTextColumn Header="سلول" Binding="{Binding CellCode}" Width="80" />
                                <DataGridTextColumn Header="تیپ تابلو" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="تعداد" Binding="{Binding QuantityOfPanels}" Width="70" />
                                <DataGridTemplateColumn Header="هزینه کل (ریال)" Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><TextBlock Text="{Binding TotalLineCost, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="اصلاحیه" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Style="{StaticResource BadgeWarningStyle}" Visibility="{Binding HasOverride, Converter={StaticResource TruthyToVisibility}}">
                                                <TextBlock Text="دارد" Style="{StaticResource BadgeTextStyle}" />
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="" Width="80">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="حذف" Style="{StaticResource SecondaryButtonStyle}" Padding="8,3"
                                                    Command="{Binding DataContext.RemoveLineCommand, ElementName=Root}" CommandParameter="{Binding}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,16,0,0" Visibility="{Binding SelectedLine, Converter={StaticResource TruthyToVisibility}}">
                            <StackPanel>
                                <TextBlock Text="اصلاحیه دستی (بند ۱۴) — نیازمند دلیل" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0"><TextBlock Text="فیلد" Style="{StaticResource CaptionTextStyle}" /><ComboBox ItemsSource="{Binding OverridableFields}" SelectedItem="{Binding OverrideFieldName}" Margin="0,4,0,0" /></StackPanel>
                                    <StackPanel Grid.Column="2"><TextBlock Text="مقدار جدید" Style="{StaticResource CaptionTextStyle}" /><TextBox Text="{Binding OverrideNewValue}" FlowDirection="LeftToRight" Margin="0,4,0,0" /></StackPanel>
                                </Grid>
                                <TextBlock Text="دلیل اصلاحیه *" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding OverrideReason}" Margin="0,4,0,10" />
                                <Button Content="ثبت اصلاحیه" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding OverrideLineCommand}" HorizontalAlignment="Left" Margin="0,0,0,16"
                                        IsEnabled="{Binding IsMutable}" />

                                <TextBlock Text="تاریخچه اصلاحیه‌های این ردیف" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,8" />
                                <DataGrid ItemsSource="{Binding OverrideHistory}" MaxHeight="180">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="فیلد" Binding="{Binding FieldName}" Width="140" />
                                        <DataGridTextColumn Header="مقدار قبلی" Binding="{Binding OldValue}" Width="100" />
                                        <DataGridTextColumn Header="مقدار جدید" Binding="{Binding NewValue}" Width="100" />
                                        <DataGridTextColumn Header="دلیل" Binding="{Binding Reason}" Width="*" />
                                        <DataGridTextColumn Header="کاربر" Binding="{Binding UserName}" Width="110" />
                                    </DataGrid.Columns>
                                </DataGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ===================== Tab: Generated BOM ===================== -->
            <TabItem Header="BOM تولید شده">
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="20" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="اقلام BOM الکتریکی ردیف انتخابی" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,10" />
                        <DataGrid ItemsSource="{Binding SelectedLine.BomItems}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="کد تجهیز" Binding="{Binding EquipmentCode}" Width="90" />
                                <DataGridTextColumn Header="شرح" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="تعداد" Binding="{Binding AdjustedQuantityPerPanel}" Width="70" />
                                <DataGridTemplateColumn Header="هزینه (ریال)" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><TextBlock Text="{Binding LineCostIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="اقلام BODY+ES ردیف انتخابی" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,10" />
                        <DataGrid ItemsSource="{Binding SelectedLine.BodyEsItems}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="کد قطعه" Binding="{Binding ComponentCode}" Width="90" />
                                <DataGridTextColumn Header="شرح" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="تعداد" Binding="{Binding AdjustedQuantityPerPanel}" Width="70" />
                                <DataGridTemplateColumn Header="هزینه (ریال)" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><TextBlock Text="{Binding LineCostIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- ===================== Tab: Consolidated MTO ===================== -->
            <TabItem Header="MTO تجمیعی">
                <TabControl Margin="0,16,0,0">
                    <TabItem Header="تجمیعی">
                        <DataGrid ItemsSource="{Binding Mto.Combined}" Margin="0,12,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="کد" Binding="{Binding Code}" Width="90" />
                                <DataGridTextColumn Header="شرح" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="واحد" Binding="{Binding Unit}" Width="60" />
                                <DataGridTextColumn Header="تعداد کل" Binding="{Binding TotalRequiredQuantity}" Width="90" />
                                <DataGridTemplateColumn Header="هزینه کل (ریال)" Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><TextBlock Text="{Binding TotalProcurementCostIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="الکتریکی">
                        <DataGrid ItemsSource="{Binding Mto.Electrical}" Margin="0,12,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="کد" Binding="{Binding Code}" Width="90" />
                                <DataGridTextColumn Header="شرح" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="تعداد کل" Binding="{Binding TotalRequiredQuantity}" Width="90" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="BODY+ES">
                        <DataGrid ItemsSource="{Binding Mto.BodyEs}" Margin="0,12,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="کد" Binding="{Binding Code}" Width="90" />
                                <DataGridTextColumn Header="شرح" Binding="{Binding Description}" Width="*" />
                                <DataGridTextColumn Header="تعداد کل" Binding="{Binding TotalRequiredQuantity}" Width="90" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </TabItem>
                </TabControl>
            </TabItem>

            <!-- ===================== Tab: Cost Breakdown ===================== -->
            <TabItem Header="شکست هزینه">
                <DataGrid ItemsSource="{Binding Revision.Lines}" Margin="0,16,0,0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="سلول" Binding="{Binding CellCode}" Width="70" />
                        <DataGridTemplateColumn Header="هزینه تجهیزات" Width="110">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding EquipmentCostPerPanel, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="هزینه BODY+ES" Width="110">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding BodyEsCostPerPanel, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="سایر هزینه‌ها" Width="110">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding OtherDirectCostPerPanel, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="هزینه کل هر تابلو" Width="120">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding TotalCostPerPanel, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="قیمت فروش هر تابلو" Width="130">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding SellingPricePerPanel, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="سود" Width="110">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding ProfitIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="حاشیه سود" Width="100">
                            <DataGridTemplateColumn.CellTemplate><DataTemplate><TextBlock Text="{Binding GrossMargin, Converter={StaticResource Percent}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate></DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="تطبیق" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <Border Style="{StaticResource BadgeSuccessStyle}" Visibility="{Binding ReconciliationPassed, Converter={StaticResource TruthyToVisibility}}"><TextBlock Text="موفق" Style="{StaticResource BadgeTextStyle}" /></Border>
                                        <Border Style="{StaticResource BadgeErrorStyle}" Visibility="{Binding ReconciliationPassed, Converter={StaticResource FalsyToVisibility}}"><TextBlock Text="ناموفق" Style="{StaticResource BadgeTextStyle}" /></Border>
                                    </Grid>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- ===================== Tab: TOTAL ===================== -->
            <TabItem Header="TOTAL">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                    <StackPanel>
                        <UniformGrid Columns="3" Rows="2">
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,14,14"><StackPanel><TextBlock Text="هزینه کل پروژه (ریال)" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.TotalProjectCostIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,14,14"><StackPanel><TextBlock Text="فروش کل (ریال)" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.TotalProjectSellingPriceIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14"><StackPanel><TextBlock Text="سود (ریال)" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.ProjectProfitIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Foreground="{StaticResource SuccessBrush}" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,14,0"><StackPanel><TextBlock Text="حاشیه سود ناخالص" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.ProjectGrossMargin, Converter={StaticResource Percent}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,14,0"><StackPanel><TextBlock Text="قابل پرداخت ریالی" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.TotalRialPayable, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Margin="0,8,0,0" /></StackPanel></Border>
                            <Border Style="{StaticResource CardBorderStyle}"><StackPanel><TextBlock Text="قابل پرداخت ارزی" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Revision.Totals.TotalForeignPayable, Converter={StaticResource ForeignAmount}}" Style="{StaticResource NumericTextStyle}" FontSize="20" Margin="0,8,0,0" /></StackPanel></Border>
                        </UniformGrid>

                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,6,0,0" Visibility="{Binding Revision.Totals.ApprovalBlockers, Converter={StaticResource TruthyToVisibility}}">
                            <StackPanel>
                                <TextBlock Text="موانع تأیید" Style="{StaticResource SubheadingTextStyle}" Foreground="{StaticResource ErrorBrush}" Margin="0,0,0,10" />
                                <ItemsControl ItemsSource="{Binding Revision.Totals.ApprovalBlockers}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate><TextBlock Text="{Binding}" Style="{StaticResource BodyTextStyle}" Margin="0,0,0,6" /></DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ===================== Tab: Project Revisions ===================== -->
            <TabItem Header="نسخه‌های پروژه">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                    <StackPanel>
                        <Button Content="ایجاد نسخه جدید با آخرین قیمت‌ها" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding CreateNewRevisionCommand}" HorizontalAlignment="Left" Margin="0,0,0,16" />

                        <DataGrid ItemsSource="{Binding RevisionHistory}" MouseDoubleClick="RevisionHistory_MouseDoubleClick" MaxHeight="240" Margin="0,0,0,20">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="نسخه" Binding="{Binding RevisionNumber}" Width="70" />
                                <DataGridTemplateColumn Header="وضعیت" Width="130">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><Border Style="{StaticResource BadgeInfoStyle}"><TextBlock Text="{Binding Status, Converter={StaticResource StatusToPersianLabel}}" Style="{StaticResource BadgeTextStyle}" /></Border></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="مبلغ فروش (ریال)" Width="140">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate><TextBlock Text="{Binding TotalProjectSellingPriceIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="تاریخ ایجاد" Binding="{Binding CreatedAtUtc, StringFormat='{}{0:yyyy/MM/dd}'}" Width="110" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Separator Margin="0,0,0,20" />
                        <TextBlock Text="مقایسه نسخه‌ها" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="*" /><ColumnDefinition Width="12" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding RevisionHistory}" SelectedItem="{Binding CompareFrom}" DisplayMemberPath="RevisionNumber" />
                            <ComboBox Grid.Column="2" ItemsSource="{Binding RevisionHistory}" SelectedItem="{Binding CompareTo}" DisplayMemberPath="RevisionNumber" />
                            <Button Grid.Column="4" Content="مقایسه" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding CompareRevisionsCommand}" />
                        </Grid>

                        <StackPanel Visibility="{Binding Comparison, Converter={StaticResource TruthyToVisibility}}">
                            <UniformGrid Columns="4" Margin="0,0,0,16">
                                <StackPanel><TextBlock Text="تغییر هزینه" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Comparison.CostDeltaIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></StackPanel>
                                <StackPanel><TextBlock Text="تغییر فروش" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Comparison.SellingPriceDeltaIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></StackPanel>
                                <StackPanel><TextBlock Text="تغییر سود" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Comparison.ProfitDeltaIrr, Converter={StaticResource IrrAmount}}" Style="{StaticResource NumericTextStyle}" /></StackPanel>
                                <StackPanel><TextBlock Text="تغییر حاشیه سود" Style="{StaticResource CaptionTextStyle}" /><TextBlock Text="{Binding Comparison.GrossMarginDelta, Converter={StaticResource Percent}}" Style="{StaticResource NumericTextStyle}" /></StackPanel>
                            </UniformGrid>
                            <DataGrid ItemsSource="{Binding Comparison.ChangedFields}" MaxHeight="220">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="سلول" Binding="{Binding CellCode}" Width="80" />
                                    <DataGridTextColumn Header="فیلد" Binding="{Binding FieldName}" Width="160" />
                                    <DataGridTextColumn Header="مقدار قبلی" Binding="{Binding OldValue}" Width="140" />
                                    <DataGridTextColumn Header="مقدار جدید" Binding="{Binding NewValue}" Width="140" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ===================== Tab: Approval ===================== -->
            <TabItem Header="بررسی و تأیید">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="اقدام" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <TextBlock Text="توضیحات" Style="{StaticResource CaptionTextStyle}" />
                                <TextBox Text="{Binding ApprovalComments}" AcceptsReturn="True" TextWrapping="Wrap" Height="56" Margin="0,4,0,14" />
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="ارسال برای تأیید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SubmitForApprovalCommand}" IsEnabled="{Binding CanSubmit}" Margin="0,0,10,0" />
                                    <Button Content="تأیید" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ApproveCommand}" IsEnabled="{Binding CanDecide}" Margin="0,0,10,0" />
                                    <Button Content="رد کردن" Style="{StaticResource DangerButtonStyle}" Command="{Binding RejectCommand}" IsEnabled="{Binding CanDecide}" Margin="0,0,10,0" />
                                    <Button Content="قفل کردن" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding LockCommand}" IsEnabled="{Binding CanLock}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="تاریخچه تأیید" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                        <DataGrid ItemsSource="{Binding ApprovalHistory}">
                            <DataGrid.Columns>
                                <DataGridTemplateColumn Header="نتیجه" Width="90">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Border Style="{StaticResource BadgeSuccessStyle}" Visibility="{Binding IsApproved, Converter={StaticResource TruthyToVisibility}}"><TextBlock Text="تأیید" Style="{StaticResource BadgeTextStyle}" /></Border>
                                                <Border Style="{StaticResource BadgeErrorStyle}" Visibility="{Binding IsApproved, Converter={StaticResource FalsyToVisibility}}"><TextBlock Text="رد" Style="{StaticResource BadgeTextStyle}" /></Border>
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="توضیحات" Binding="{Binding Comments}" Width="*" />
                                <DataGridTextColumn Header="کاربر" Binding="{Binding DecidedByUserName}" Width="120" />
                                <DataGridTextColumn Header="تاریخ" Binding="{Binding DecidedAtUtc, StringFormat='{}{0:yyyy/MM/dd HH:mm}'}" Width="150" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- ===================== Tab: Reports and Exports ===================== -->
            <TabItem Header="گزارش‌ها و خروجی‌ها">
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,16,0,0">
                    <StackPanel>
                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="پیشنهاد قیمت مشتری" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <StackPanel Orientation="Horizontal">
                                    <ComboBox Width="160" Margin="0,0,10,0" SelectedValuePath="Tag" SelectedValue="{Binding QuotationLanguage}">
                                        <ComboBoxItem Content="دوزبانه" Tag="bilingual" />
                                        <ComboBoxItem Content="فارسی" Tag="fa" />
                                        <ComboBoxItem Content="English" Tag="en" />
                                    </ComboBox>
                                    <Button Content="دریافت PDF پیشنهاد قیمت" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ExportQuotationCommand}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                            <StackPanel>
                                <TextBlock Text="گزارش قیمت داخلی" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="PDF" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportInternalCostingPdfCommand}" Margin="0,0,10,0" />
                                    <Button Content="Excel" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportInternalCostingExcelCommand}" />
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel>
                                <TextBlock Text="فهرست مواد (MTO / BOM)" Style="{StaticResource SubheadingTextStyle}" Margin="0,0,0,12" />
                                <TextBlock Text="تجمیعی" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6" />
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Content="PDF" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoPdfCommand}" CommandParameter="combined" Margin="0,0,10,0" />
                                    <Button Content="Excel" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoExcelCommand}" CommandParameter="combined" />
                                </StackPanel>
                                <TextBlock Text="الکتریکی" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6" />
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Button Content="PDF" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoPdfCommand}" CommandParameter="electrical" Margin="0,0,10,0" />
                                    <Button Content="Excel" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoExcelCommand}" CommandParameter="electrical" />
                                </StackPanel>
                                <TextBlock Text="BODY+ES" Style="{StaticResource CaptionTextStyle}" Margin="0,0,0,6" />
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="PDF" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoPdfCommand}" CommandParameter="bodyes" Margin="0,0,10,0" />
                                    <Button Content="Excel" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding ExportMtoExcelCommand}" CommandParameter="bodyes" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
